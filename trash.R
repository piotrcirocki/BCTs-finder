
# jesli taki sam chromosom z tej samej strony znajduje się w dotychczasowych, gdzie is relevant false i taka sama
# nazwa chromosomu trzeba nagradzać wystąpione wartości zerem i karać pozostałe if(dfR[i-1,0]$rname = row$rname &&
# dfR[i-1,0]$rdir == row$rdir ){ dfR[i-1,0]$countBuffer = dfR[i-1,0]$countBuffer +1


# } w przeciwnym wypadku, jeśli nowy else{ appendDf(row, dfR)


# znależć elementy niezakwalifikowane jako nieprawidłowe w df oraz elementy jeszcze się kwalifikujące i doliczyć im
# tresholdElement tym ktore przekroczą treshold ustawić isRelevant na false }
